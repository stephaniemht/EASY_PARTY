<%= simple_form_for [@event, @event.items.new], remote: true, html: { id: "item_form", class: "d-flex" } do |form| %>
  <div class="form-group">
    <%= form.input :content, label: false, placeholder: "Tu amènes quoi le sang ?", input_html: { required: true, class: "flex-grow-1" } %>
  </div>
  <%= form.submit "Ajouter l'item", class: "btn btn-primary" %>
<% end %>